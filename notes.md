refactor. 
If user = inactive 'n methods are deactivated. if ser are active = methods activated.
seed rooms on dbcpntext.

states and events, onclick, controlled ipruts

react, NextJS mderstand for onjo exp. 

work on an app and try to implement NexJS

get by element(ID)

(windom begins when yo realize 
props=properties.every one is your mentor/teacher.)

25/2/25 
signal.R webhooks
    UI refresh patterns
    Intergrate S.R with React.
        lifecycle of a real-time update(connction, subscription, action, cleanup)(always on)
        (does it use toasts for notification  and stores the changes in a BD)
(sillo; code in class.)

include cookies to store authorize
    in the way, will have put = 'false'.

(note: remove the icons in the code)

map inside api.jsx
usclac - logic
e.preventDelfalt search.why.

(ctrl + I : to prompt)
